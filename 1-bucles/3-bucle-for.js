// ej 3
// muestra por consola la tabla del 7;
// Utilizad el bucle for
var siete = 7;
for ( var tabla = 1 ; tabla <= 10 ; tabla++){
var mult = tabla * siete;
console.log(siete + " x " + tabla + "=" + mult);
}
